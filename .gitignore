node_modules/

# build output — regenerated by CI, never committed
dist/
dist-single/

# source decks rendered to images while exploring visuals. These are
# competition and client material; a public repo would publish them, so
# they stay local. Nothing in the site imports from here.
design-reference/

.DS_Store
*.local
.env
.env.*
